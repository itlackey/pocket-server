/**
 * Context injection configuration (always on)
 * Converted from TypeScript for SvelteKit with JSDoc types
 *
 * @fileoverview Configuration constants for project context loading
 */

// Context injection configuration (always on)
export const MAX_CONTEXT_BYTES = 100_000; // ~100KB cap for injected context
export const MAX_IMPORT_DEPTH = 5; // Maximum recursion depth for @imports in CLAUDE.md